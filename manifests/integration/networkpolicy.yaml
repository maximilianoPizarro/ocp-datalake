# Web Terminal runs in another project, so it must use Route churn-camel.
# The worker Service is iflow-churn-score-bridge:8080 (created by the operator).
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: churn-score-bridge-ingress
  namespace: ocp-datalake
  labels:
    app.kubernetes.io/name: churn-score-bridge
    app.kubernetes.io/part-of: ocp-datalake
spec:
  podSelector:
    matchLabels:
      platform.io/flow-name: churn-score-bridge
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              network.openshift.io/policy-group: ingress
        - podSelector: {}
      ports:
        - protocol: TCP
          port: 8080
