# Applied by scripts/enable-integration.sh after the operator CRD exists.
# Not in the repo-root kustomization.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ocp-datalake
resources:
  - flow.yaml
  - networkpolicy.yaml
  - route.yaml
labels:
  - pairs:
      app.kubernetes.io/part-of: ocp-datalake
    includeSelectors: false
