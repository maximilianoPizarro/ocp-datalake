apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - postgres-pvc.yaml
  - postgres-service.yaml
  - postgres-deployment.yaml
  - secrets-bootstrap.yaml
# postgres-creds / maas-db-config values are created by the PreSync Job or scripts/enable-maas.sh, not stored in git.
