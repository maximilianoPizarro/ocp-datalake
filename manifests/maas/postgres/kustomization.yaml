apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - postgres-pvc.yaml
  - postgres-service.yaml
  - postgres-deployment.yaml
# postgres-creds and maas-db-config are created by scripts/enable-maas.sh (not in git).
