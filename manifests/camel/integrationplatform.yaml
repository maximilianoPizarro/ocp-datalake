# Created in ocp-datalake once the Camel K operator CSV is Succeeded.
# OpenShift defaults (internal registry) are filled by the operator.
apiVersion: camel.apache.org/v1
kind: IntegrationPlatform
metadata:
  name: camel-k
  namespace: ocp-datalake
  labels:
    app.kubernetes.io/part-of: ocp-datalake
spec: {}
